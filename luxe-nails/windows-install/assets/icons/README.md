# App Icon Files

Place your production icon file here before building:

- `app.icns` for macOS

The build script (`build_executable.py`) automatically uses these when present.

Recommended source size: 1024x1024 square logo exported to `.icns`.
